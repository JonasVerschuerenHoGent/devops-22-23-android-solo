package com.example.test.domain

import java.util.*
import kotlin.collections.ArrayList

class MemberMock {/*
    var members: ArrayList<Member>
    init{
        members = createMembersMockArray()
    }
    private fun createMembersMockArray(): ArrayList<Member>{
        var random: Random = Random();

        var departments = Department.values()
        var roles = Role.values()

        var output = ArrayList<Member>();

        for(i in 0..19){
            output.add(Member(
                i,
                "Member ${i}",
                true,
                "Email ${i}",
                "Phone number ${i}",
                departments[random.nextInt(departments.size-1)],
                roles[random.nextInt(roles.size-1)]
            ))
        }
        return output
    }
*/
}